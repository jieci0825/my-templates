const PREFIX = 'vue3-admin:'

export const STORAGE_KEY = {
    ACCESS_TOKEN: `${PREFIX}_access_token`,
    REFRESH_TOKEN: `${PREFIX}_refresh_token`,
    USER_INFO: `${PREFIX}_user_info`,
    SETTINGS: `${PREFIX}_settings`,
    TABS: `${PREFIX}_tabs`,
    LOCK_INFO: `${PREFIX}_lock_info`,
}
